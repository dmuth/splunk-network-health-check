
version: "3"

services:

  splunk-network-health-check:
    build:
      context: .
    restart: "unless-stopped"
    privileged: true
    volumes:
      - .:/mnt
      - ./splunk-data:/data
    environment:
      - TZ=EST5EDT
    ports:
      - 8000:8000


